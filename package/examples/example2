\dontrun{
# Change to noBurnInIterations=500, noIterations=5000 for better results
# but longer run times. The numbers here are only useful for code tests.
example2_lgss(noBurnInIterations=50, noIterations=200)
}